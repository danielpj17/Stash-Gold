.next/
node_modules/
.env.local
.env.*
!.env.example

# TypeScript incremental build cache — regenerated on every build, so tracking
# it just produces churn in every diff.
tsconfig.tsbuildinfo
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
